{
  "name": "ftp-uploader",
  "version": "1.0.0",
  "description": "SFTP upload CLI skill with natural language parsing and local credential memory",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ftp-uploader": "dist/index.js"
  },
  "files": [
    "dist",
    "src",
    "tsconfig.json",
    "skills"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Rayzzhu/ftp-uploader.git"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "start": "node dist/index.js",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["sftp", "skill", "cli", "agent-skills"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ssh2-sftp-client": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ssh2-sftp-client": "^9.0.6",
    "typescript": "^5.7.2"
  }
}
